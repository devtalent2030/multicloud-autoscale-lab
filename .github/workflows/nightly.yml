name: weekly-refresh
on:
  schedule:
    - cron:  '0 3 * * 5'   # every Friday 03:00 UTC
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Repo heartbeat
        run: echo "Weekly refresh $(date -u)"
